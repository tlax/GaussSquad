import UIKit

class MHelpItem
{
    let reusableIdentifier:String
    let image:UIImage
    let title:String
    
    init(reusableIdentifier:String, image:UIImage, title:String)
    {
        self.reusableIdentifier = reusableIdentifier
        self.image = image
        self.title = title
    }
}
